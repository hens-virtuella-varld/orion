import FilterButton from "./FilterButton";
import SearchBar from "./SearchBar";

const Filter = () => {
	return (
		<div>
			<SearchBar />
			<FilterButton />
			<FilterButton />
			<FilterButton />
		</div>
	);
};

export default Filter;
